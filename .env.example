# Example environment variables for the project
# Set STREAMLIT_PORT to run Streamlit on a custom port
STREAMLIT_PORT=8501
# Path to the static family JSON used by the static site
FAMILY_JSON_PATH=data/family.json
# Toggle verbose logging for local debugging
VERBOSE=false
